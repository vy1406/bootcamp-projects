// Create instances of your classes
// Create the loadData and renderData functions - these should use the relevant instance

const renderer = new Renderer()
const apiManager = new APIManager(renderer)

apiManager.loadData()


